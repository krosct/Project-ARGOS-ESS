# Argos — Checagem (React + Vite + Tailwind)

Quick start:

1. `npm install`
2. `npm run dev`

This project is a single-page app converted from an HTML prototype into a modular React + Vite structure with TailwindCSS.
